server:
  port: 8025

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: "http://localhost:8024/eureka"
    fetch-registry: true
    register-with-eureka: true
